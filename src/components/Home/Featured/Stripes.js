import React from 'react'
import { easePlyOut } from 'd3-ease'
import Animate from 'react-move/Animate'

const Stripes = () => {
  const showStripes = () => <div>stripes</div>

  return <div className='featured_stripes'>{showStripes()}</div>
}

export default Stripes
